{
  "name": "cligpt",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "type": "module",
  "bin":  "./dist/main.js",
  "scripts": {
    "build": "esbuild --bundle src/main.ts --platform=node --outfile=dist/main.js --packages=external --format=esm",
    "local-link": "yarn unlink --global | true && yarn  build && yarn link --global"
  },
  "devDependencies": {
    "@types/node": "^20.11.27",
    "esbuild": "^0.20.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.2"
  },
  "dependencies": {
    "commander": "^12.0.0"
  }
}
